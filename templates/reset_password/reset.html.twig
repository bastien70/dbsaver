{% extends '@EasyAdmin/login_layout.html.twig' %}

{% block content %}
    <h3>{{ 'reset_password.reset.title'|trans }}</h3>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button type="submit" name="_submit" class="btn btn-primary">{{ 'reset_password.reset.submit'|trans }}</button>
    {{ form_end(resetForm) }}
{% endblock %}
